<?php require 'templates/layouts/header.phtml'?>

<div class="container">
    <h1><?= $libro ? 'Editar' : 'Agregar' ?></h1>
    <form action="<?= $libro ? BASE_URL . 'editar-libro/' . $libro->id_libro : 'agregar-libro' ?>" method="POST">
        <div class="mb-3">
            <label for="Titulo" class="form-label">Título</label>
            <input type="text" class="form-control" name="Titulo" value="<?= $libro ? $libro->Titulo : '' ?>">
        </div>
        <div class="mb-3">
            <label for="Autor" class="form-label">Autor</label>
            <input type="text" class="form-control" name="Autor" value="<?= $libro ? $libro->Autor : '' ?>">
        </div>
        <div class="mb-3">
            <label  for="Reseña"class="form-label">Reseña</label>
            <input type="text" class="form-control" name="Reseña" value="<?= $libro ? $libro->Reseña : '' ?>">
        </div>
        <div class="mb-3">
            <label class="form-label" for="Año">Año de Publicación</label>
            <input type="number" class="form-control" name="Año" value="<?= $libro ? $libro->Año: '' ?>">
        </div>
        <div class="mb-3">
            <label class="form-label" for="ID_genero">Genero</label>
            <select name="ID_genero">
                <?php foreach ($generos as $genero) : ?>
                    <option value="<?= $genero->ID_genero ?>" <?= $libro && $libro->ID_genero == $genero->ID_genero ? 'selected' : '' ?>><?= $genero->Nombre ?></option>
                <?php endforeach; ?>
            </select>
        </div>
        <button type="submit" class="btn btn-success">Enviar</button>
    </form>
</div>

<?php require 'templates/layouts/footer.phtml'?>


